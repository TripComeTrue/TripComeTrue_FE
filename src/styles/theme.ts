import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#B4F34C',
    },
  },
  typography: {
    fontFamily: "'AppleSDGothicNeo', 'SF-Pro', 'sans-serif'",
  },
});

export default theme;
