import styled from 'styled-components';

const MyPageWishListMoreWrap = styled.div`
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 1rem;
  padding: 1.25rem;
`;

export default MyPageWishListMoreWrap;
