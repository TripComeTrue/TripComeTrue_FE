import styled from 'styled-components';

const MyPageReviewWrap = styled.div`
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 0 1rem;
  padding: 1.25rem;
`;

export default MyPageReviewWrap;
