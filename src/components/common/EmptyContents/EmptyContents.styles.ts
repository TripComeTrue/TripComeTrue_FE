import styled from 'styled-components';
import { alignCenter } from '@/styles/common';

export const EmptyContentsWrapper = styled.div`
  ${alignCenter}
  flex-direction: column;
  justify-content: center;
  gap: 0.8rem;

  height: 7rem;

  margin: 2rem 0 1.5rem 0;
`;
