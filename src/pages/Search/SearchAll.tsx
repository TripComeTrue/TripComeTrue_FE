import { SearchCity, SearchCreator, SearchSpot } from '@/components/Search';

const SearchAll = () => {
  return (
    <div>
      <SearchCity />
      <SearchSpot />
      <SearchCreator />
    </div>
  );
};

export default SearchAll;
