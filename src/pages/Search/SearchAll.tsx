import { SearchCity, SearchCreator } from '@/components/Search';

const SearchAll = () => {
  return (
    <div>
      <SearchCity />
      <SearchCreator />
    </div>
  );
};

export default SearchAll;
